# Daily Tech Signal — pipeline dependencies (Python 3.11+)
# Pinned with lower bounds for stability; CI caches these.

feedparser>=6.0.11        # RSS/Atom parsing
requests>=2.32.0          # sync HTTP (feed fetch, AI providers)
httpx>=0.27.0             # async link validation (HEAD/GET)
pydantic>=2.7,<3          # schema-first validation
PyYAML>=6.0.1             # sources/events/history YAML
Jinja2>=3.1.4             # deterministic Markdown rendering
python-dateutil>=2.9.0    # robust date parsing
beautifulsoup4>=4.12.3    # HTML cleaning of feed summaries
tenacity>=8.3.0           # retry logic for AI provider calls
tzdata>=2024.1            # IANA tz database for zoneinfo (America/New_York)
